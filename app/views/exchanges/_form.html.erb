<%= form_for(@exchange) do |f| %>
  <% if @exchange.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exchange.errors.count, "error") %> prohibited this exchange from being saved:</h2>

      <ul>
      <% @exchange.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exchange_type %><br>
    <%= f.text_field :exchange_type %>
  </div>
  <div class="field">
    <%= f.label :exchange_value %><br>
    <%= f.text_field :exchange_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
